# 实现与验收记录

验证日期：2026-09-16。要求来源：`REQUIREMENTS.md`。

## Emoji、语速和互动练习

- 本地安装固定版本 `@twemoji/svg@15.0.0`（3,720 个 SVG）与 `emojibase-data@17.0.0` 英文/德文名称。服务端查找并提供本地素材；前端按白板区域放大显示。bus/train/sofa 的有标签形状及“单个空形状 + 单个已教词”得到正确 emoji；真实色球/基础几何保留。图形工具增加 emoji 类型和素材查询工具。
- `node --test test/visuals.test.js` 首次三个目标用例失败（实际 shape，期望 emoji），修复后通过。没有匹配的 emoji 会显示明确的 GPT Image 等待提示并异步生成；图片加载前不会评估看图回答，失败可改为口头练习。继续保留旧步骤图片不覆盖新白板的保护。
- 新增 `show_practice` 工具：跟读、德语→英语选择、图片→英语口说、德语→英语口说；固定布局避免口头题直接显示答案，颜色练习继续使用准确颜色的球。口头题禁用点击作答，支持未听清、明确答错、提示后重试，跟读记录不参与独立掌握判断。沿用当前数据库结构，无版本迁移。
- 五档 Sprechtempo 滑条，默认 Normal，浏览器记住偏好，重连时传入初始提示。按 [OpenAI Live 语音提示指南](https://developers.openai.com/api/docs/guides/live-prompting) 和 [instructions.append 协议](https://developers.openai.com/api/reference/typescript/resources/live) 发送简短语速指令，收到确认后提示从下一句生效。请求去抖并串行，失败显示重试；不是精确音频倍速，未测量实际每分钟词数。
- 保守改善：减少重复长反馈，已教词的普通跟读补规划由约 3.5 秒缩到 2.2 秒；明确答案仍为 1.7 秒，其他普通碎片仍为 3.5 秒。只触发后台判断，不从字幕直接计分，不改变沉默等待与网络清理。
- **52 项 Node 测试、18 项浏览器测试及生产构建通过**。新增覆盖本地 SVG 可访问、未知词不乱配素材、异步图片等待、练习题布局/评分/去重、跟读不计掌握、语速范围/确认/失败保持、刷新记忆；现有备课、转写、断线恢复、历史记录等回归通过。1280×620 大 emoji/滑条截图 `.cache/classroom-emoji-tempo.png` 已目视检查；1280×720、1280×620 布局和 390px 窄屏也通过。
- **真实 API**：`node scripts/smoke-teaching-materials.js` 在内存库验证 bus/train/sofa 及四种练习，全部成功；`node scripts/smoke-live.js` 验证 WebRTC、后台白板、字幕、不断线调整语速和释放媒体；`node scripts/smoke-live.js --conversation` 验证合成德语要求选择题、点击答错一次、口说正确答案并保存。
- 完整语音测试首次发现“明确要求选择题仍继续跟读”。补充明确练习请求优先于默认教学顺序，并允许无错误关闭当前跟读后，同一真实测试通过。未通过修改测试输入绕过该问题。
- 测试使用隔离课堂/内存数据和合成音频，未修改正式课程内容。本轮未调用真实图片生成服务（异步接入与失败/过期保护通过自动测试），未验证真实儿童声音、长时间课堂体验及五档语速的主观听感。
- 正式服务已在无活动课堂/备课请求时通过启动脚本重启，健康检查与本地公交车 SVG 返回正常。重启前后均为 11 节历史课堂、12 个主题，原有学习记录保留。

## 备课语音输入

- 新增 **Spracheingabe** 录音按钮、停止/取消、时长与处理提示，转写文字追加到现有草稿，由用户检查后发送。默认自动识别中文/英文/德文，也可手动指定；中文使用 `zh`，接口兼容输入别名 `cn`。
- `.env` / `.env.example` 新增 `ENGLISH_TRANSCRIPTION_MODEL=gpt-transcribe`，健康检查与设置页显示实际加载值。按 [官方转写接口](https://developers.openai.com/api/reference/typescript/resources/audio/subresources/transcriptions/methods/create) 使用 multipart 文件上传；自动模式为 `gpt-transcribe` 提供三个 `languages[]`，其他模型系列使用自身语言检测，避免携带专属参数。
- 38 项本地测试、17 项浏览器测试与生产构建通过。本地检查覆盖 `.env` 与进程环境优先级、multipart 音频字节/扩展名/MIME、三语言参数与模型切换、空转写、上游错误脱敏、非法语言/格式、20 MiB 上限、跨站拒绝和断开请求后取消上游调用。
- 浏览器使用合成麦克风和真实 MediaRecorder，确认上传的是 WebM（服务端收到 EBML 文件头），转写追加草稿且不会自动发送备课消息。覆盖取消、切换页面释放麦克风、权限拒绝、取消权限等待后迟到的音频流清理、失败保留草稿并可重试、3 分钟自动停止。
- 1280×620 下按钮、语言选择、输入框和发送按钮保持可见，已目视检查 `.cache/preparation-dictation.png`；390px 窄屏无横向溢出。
- 真实 `node scripts/smoke-transcription.js .cache/test-speech/question-transcription.webm` 成功：使用现有合成德语 WAV 转成单声道 64 kbps WebM/Opus（88,109 字节），通过实际本地 HTTP 转写路由调用 `gpt-transcribe`，返回德语文本。未访问真实麦克风、未写入正式课堂或备课记录；其中一个词识别不准确，仍需用户检查转写结果。
- 未实测真实麦克风/噪声条件、真人中英德混说，以及 Safari 的备用录音格式。音频仅在内存处理，本地不落盘；转写请求会发送至 OpenAI。测试音频为合成固定样本。
- 本地服务已在无活动课堂和备课请求时重启；健康检查确认加载 `gpt-transcribe`，空音频正确返回 400。重启前后均为 7 节课堂、7 个主题、1 条备课记录。

## 备课聊天清空与主题删除

- 34 项本地测试、11 项浏览器测试及生产构建通过。覆盖聊天清空的确认/取消、旧聊天不再进入下一次模型输入、过期页面请求被拒绝、备课进行中禁止清空/删除、AI 删除提议的明确确认、版本冲突、删除后重启不恢复、历史课堂保留、删空主题后首页可用。
- AI 的 `request_topic_deletion` 使用 [OpenAI 官方函数调用格式](https://developers.openai.com/api/docs/guides/function-calling)，仅生成待确认项，不能直接执行删除。确认接口校验主题版本及聊天中的实际提议。手动卡片删除使用同一确认与删除流程。
- 真实 `node scripts/smoke-preparation.js --delete` 已通过：模型为 Hallo, Welt! 生成删除提议，确认前主题仍存在，确认后删除，清空聊天后主题仍保持删除。只使用内存测试库，未删除正式主题。
- 数据库版本升级为 3：保存删除标记，防止默认主题重启后重新出现；聊天清空会持久化新的会话版本，删除原备课记录。正式数据库的一致性快照已保存至 `data/backups/learning-before-preparation-1789564378927.sqlite`，其独立副本完成版本 2→3 升级与清空后重启检查。
- 1280×620 CSS 视口下已目视检查备课清空按钮和 AI 删除确认项，输入框和发送按钮保持可见；浏览器测试也覆盖窄屏。测试中的两处断言已修正：历史入口打开总结页；删空循环必须先等待首页主题加载。
- 本地服务已重启，健康检查正常，原有 7 节课堂、8 个主题、5 条备课记录均保留。清空和删除由用户点击后执行。

## 删除无人回应超时功能

- 按最新要求移除在场提醒、60 秒静默断开、活动上报接口及“我还在”按钮，删除等待期间阻止后台规划的相关条件。提示词要求耐心等待回答与后台结果，不例行询问是否还在。
- 回归测试先复现旧行为：后台工作时仍在 20/40 秒发出两次提醒。移除后模拟 120 秒无用户回应且浏览器心跳正常，确认没有提醒、没有关闭会话、没有取消后台工作，学习记录保留；实际断线仍清理连接。
- 29 项本地测试、7 项浏览器测试、生产构建全部通过。浏览器覆盖 FHD 150% 等效视口、长字幕、实际断线提示、媒体释放与重连、备课聊天。此次未额外调用真实语音 API。
- 正式服务已在无活动课堂时重启，健康检查正常，7 节已有课堂记录保留，新前端资源已生效。手动结束、约 10 分钟正常下课和真正断线后的清理机制保留。

## 跟读继续教学、模型配置与备课聊天更新

- 最终验证：31 项本地测试、7 项浏览器测试、生产构建全部通过；正式服务已使用重启入口重新启动，备课接口和模型配置正常，原有 5 节课堂记录保留。数据库升级前已创建一致性快照至 `data/backups/`。
- “Monday → Klasse 后停住”的最小复现最初失败：`node --test --test-name-pattern='repeating Monday' test/classroom.test.js` 返回后台规划次数 0（期望 1）。原因是原有字幕补触发只覆盖选择题选项和少数关键词，普通跟读没有触发；现普通话语在字幕稳定约 3.5 秒后补触发，已有语音委托时去重，并明确要求跟读后提供下一步。
- 已运行真实 `node scripts/smoke-live.js --repeat`：合成语音说出 Monday 后，白板和老师字幕继续到 Tuesday，学习记录保存 Tuesday，提前结束释放媒体。未录制真实麦克风。
- 已运行真实 `node scripts/smoke-preparation.js`：后台 AI 在内存测试库完善七天安排、给颜色补充 purple/orange、新建可开课的水果主题，持久化操作校验通过；不修改正式主题。
- 本地检查覆盖 `.env` 模型读取与环境优先级、密钥不从文件加载、主题创建/修改/重启保留、每课主题快照、失败不部分保存、重复请求/并发/版本冲突、课后总结使用真实已存结果、旧数据库升级，以及白板擦除不丢学习记录。
- 浏览器检查覆盖备课聊天提交、修改结果显示、刷新后聊天保留、新主题出现在首页并可开始课堂；1280×620 下输入框和发送按钮无需整页滚动，390px 窄屏无横向溢出。
- 模型按 [OpenAI Live 委托文档](https://developers.openai.com/api/docs/guides/live-delegation) 接收后台返回的下一步口头提示。真实声音、网络和模型输出仍可能影响等待时间，本次实测验证了所述 Monday 跟读路径。

## FHD 150% 更新

- 1280×720 和 1280×620 CSS 视口均验证课堂整页无纵向溢出，答案、提示、结束和重连操作保留在首屏。长字幕在对话面板内滚动；断线说明出现后仍不撑高页面。真实课堂与测试课堂截图均已目视检查。
- 原因：白板的宽高比/最小高度与题目高度叠加，原 720px 视口下页面达到 922px；对话区也有固定最小高度。现桌面课堂按窗口剩余高度分配白板和对话空间，小高度窗口缩减留白与选项高度，超长内容保留面板内滚动。
- 本次未改变 Windows 显示设置；用等效 CSS 视口验证 FHD 150% 和浏览器工具栏占用。
- 会话关闭沿用官方 Live 的 `session.close` 控制：[OpenAI Live 会话管理](https://developers.openai.com/api/docs/guides/live-conversations)。

## 已执行并通过

- 三个指定模型的账号访问检查；密钥仅从现有系统环境变量加载。
- GPT-5.6 Terra 的实际 Responses 调用。
- 真实 GPT-Live WebRTC 连接，双方语音转写，Terra 实际调用白板工具。
- 合成德语语音提出选择题请求，后台实际生成题目；点击错误选项保存一次错误，随后口头说出正确颜色，Terra 识别、评估并保存语音回答。
- 提前结束后释放媒体资源，保存课程状态和学习内容。
- GPT Image 2.5 Flare 实际生成并保存 1024 × 1024 PNG，验证文件格式、尺寸并目视检查。
- 18 项本地自动检查：白板原子更新与回滚、事件重放、点击/语音去重（含后台延迟）、不确定答案、迟到答案、跨步骤语音归属、提示重试、掌握证据、重复学习、中断恢复、重启持久化、字幕修正、图片过期/失败、总结失败及本机访问边界。
- 3 项浏览器检查：首页和设置/历史、课堂 60/40 布局及断线禁止答题、手机窄屏无横向溢出。
- 生产构建、Windows 启动脚本与本地服务启动检查。

## 测试中修复的问题

- 语音委托与快速回答交错时，增加对明确选项/课堂请求的后台复核入口；不会仅凭字幕片段直接计分。
- 新请求出现后，不再播报旧规划任务返回的过时讲解。
- 同一段语音跨白板切换仍绑定原题，重复后台委托共用同一答案事件标识。
- 点击/语音合并依据实际接收时间，避免模型响应较慢时重复计分。
- 等待旧会话清理完成再重连，防止旧任务清理新连接；明确结束与断线清理同时发生时保留正确的课程结束状态。
- 自动收尾要等到语音结果已交付并观察到老师发言，再等待实际音频/字幕活动停止。
- 第二次启动端口已占用时，不执行数据库中断恢复，避免影响已有课堂。

## 尚需真实使用验证的体验

- 儿童实际声音、不同麦克风/房间噪声下的识别与插话听感。
- 一节完整约 10 分钟课程的节奏、词量和自然收尾体验。
- GPT-Live 没有精确逐字播放对齐事件；字幕使用收到的时间片段，无法保证被打断时每个字都与扬声器播放完全一致。

真实测试使用单独的临时数据库与合成音频，未向正式学习档案写入测试成绩，未录制真实麦克风。界面不包含模拟成绩或模拟 AI 回复。
